export interface slideData {
    imgSrc: string,
    linkTo: string,
    title: string,
    viewerCount: number,
    originalPrice: number,
    currnetPrice: number,
}

export type carouselType = "banner" | "streaming" | "item"